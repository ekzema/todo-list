<%= form_for(@todolist, remote: true,  :html => {class: "formTodo"}) do |f| %>   
    <%= f.text_field :name %>     
    <div class="actions">
      <%= f.submit "Новый список", class: "btn btn-success  " %>
    </div>
<% end %>
